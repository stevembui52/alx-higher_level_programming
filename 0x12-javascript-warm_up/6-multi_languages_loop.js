#!/usr/bin/node
/* Script that prints 3 lines, and one loop */

const array = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < array.length; i++) {
  console.log(array[i]);
}
